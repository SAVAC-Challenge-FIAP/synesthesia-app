# Chaves opcionais — o app degrada graciosamente sem elas (Deezer não exige chave).
# Copie para .env e preencha. Nunca commitar o .env.

# Gemini (curadoria musical com justificativas geradas por IA)
EXPO_PUBLIC_GEMINI_API_KEY=

# Last.fm (enriquecimento de metadados — reservado para próximas sprints)
EXPO_PUBLIC_LASTFM_API_KEY=
